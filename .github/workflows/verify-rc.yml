name: verify-rc

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    branches:
      - main
    paths:
      - .github/workflows/verify-rc.yml
      - Makefile
      - build.gradle.kts
      - settings.gradle.kts
      - gradle.properties
      - gradle/**
      - ci/**
      - scripts/**
      - shared/**
      - androidApp/**
      - iosApp/**
      - docs/release-gate.md
      - docs/appstore-review-checklist.md
  push:
    branches:
      - main
    paths:
      - .github/workflows/verify-rc.yml
      - Makefile
      - build.gradle.kts
      - settings.gradle.kts
      - gradle.properties
      - gradle/**
      - ci/**
      - scripts/**
      - shared/**
      - androidApp/**
      - iosApp/**
      - docs/release-gate.md
      - docs/appstore-review-checklist.md
  schedule:
    - cron: "0 3 * * 1-5"
  workflow_dispatch:
    inputs:
      verify_profile:
        description: Device pool profile to run
        required: false
        type: choice
        default: rc-full
        options:
          - ci-pr
          - ci-nightly
          - rc-full

concurrency:
  group: verify-rc-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  verify-rc:
    if: github.event_name != 'pull_request' || !github.event.pull_request.draft
    runs-on: macos-15
    timeout-minutes: 180
    env:
      VERIFY_PROFILE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.verify_profile || (github.event_name == 'schedule' && 'rc-full' || 'ci-pr') }}
      ANDROID_SDK_ROOT: /Users/runner/Library/Android/sdk
    steps:
      - name: Schedule gate precheck
        id: schedule_gate
        shell: bash
        run: |
          set -euo pipefail
          echo "== verify-rc precheck =="
          echo "event: ${{ github.event_name }}"
          echo "ref: ${{ github.ref }}"
          echo "sha: ${{ github.sha }}"
          echo "requested profile: $VERIFY_PROFILE"
          echo "schedule gate variable ENABLE_RC_FULL_SCHEDULE='${{ vars.ENABLE_RC_FULL_SCHEDULE }}'"
          if [[ "${{ github.event_name }}" == "schedule" && "${{ vars.ENABLE_RC_FULL_SCHEDULE }}" != "true" ]]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "Skipping scheduled rc-full run: ENABLE_RC_FULL_SCHEDULE is not 'true'."
          else
            echo "enabled=true" >> "$GITHUB_OUTPUT"
            if [[ "${{ github.event_name }}" == "schedule" ]]; then
              echo "Scheduled rc-full run is enabled by ENABLE_RC_FULL_SCHEDULE=true."
            else
              echo "Non-schedule event; proceeding with verify-rc workflow."
            fi
          fi
          echo "effective profile: $VERIFY_PROFILE"

      - name: Checkout
        if: steps.schedule_gate.outputs.enabled == 'true'
        uses: actions/checkout@v4

      - name: Workflow context
        if: steps.schedule_gate.outputs.enabled == 'true'
        shell: bash
        run: |
          set -euo pipefail
          echo "== verify-rc workflow context =="
          echo "runner: $RUNNER_NAME"
          echo "runner os: $RUNNER_OS"
          echo "runner arch: $RUNNER_ARCH"
          echo "workspace: $GITHUB_WORKSPACE"
          echo "profile: $VERIFY_PROFILE"
          echo "android sdk root: $ANDROID_SDK_ROOT"
          echo "repository: $GITHUB_REPOSITORY"
          echo "run id: $GITHUB_RUN_ID"
          echo "run attempt: $GITHUB_RUN_ATTEMPT"
          echo "actor: $GITHUB_ACTOR"

      - name: Set up JDK 21
        if: steps.schedule_gate.outputs.enabled == 'true'
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Accept Android SDK licenses
        if: steps.schedule_gate.outputs.enabled == 'true'
        shell: bash
        run: |
          set -euo pipefail
          SDKMANAGER_BIN=""
          for candidate in \
            "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" \
            "$ANDROID_SDK_ROOT/cmdline-tools/bin/sdkmanager" \
            "$ANDROID_SDK_ROOT/tools/bin/sdkmanager"; do
            if [[ -x "$candidate" ]]; then
              SDKMANAGER_BIN="$candidate"
              break
            fi
          done
          if [[ -z "$SDKMANAGER_BIN" ]]; then
            echo "sdkmanager not found under $ANDROID_SDK_ROOT" >&2
            exit 1
          fi
          echo "using sdkmanager: $SDKMANAGER_BIN"
          set +o pipefail
          if ! yes | "$SDKMANAGER_BIN" --licenses >/dev/null; then
            echo "Failed to accept Android SDK licenses" >&2
            exit 1
          fi
          set -o pipefail
          echo "android sdk licenses accepted"

      - name: Provision Device Pool
        if: steps.schedule_gate.outputs.enabled == 'true'
        shell: bash
        run: |
          set -euo pipefail
          echo "== provisioning device pool =="
          echo "profile: $VERIFY_PROFILE"
          make provision-devices
          echo "device pool provisioning completed"

      - name: Verify RC Gate
        if: steps.schedule_gate.outputs.enabled == 'true'
        shell: bash
        run: |
          set -euo pipefail
          echo "== verify rc gate =="
          echo "profile: $VERIFY_PROFILE"
          echo "step command timeout seconds: ${STEP_COMMAND_TIMEOUT_SECONDS:-2400}"
          make verify-rc
          echo "verify rc gate completed"

      - name: Upload RC artifacts
        if: always() && steps.schedule_gate.outputs.enabled == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: rc-verify-artifacts
          path: build/rc-verify/
